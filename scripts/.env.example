# Copy to .env and fill in. Never commit .env
CV_API_ID=your_sandbox_api_id
CV_API_KEY=your_sandbox_api_key_base64
CV_BASE_URL=https://uatapi.cleanverse.com/api/cooperate

# Deployer key — must equal QuorumAsset.owner() for register_atoken / validator/grant / register
MONAD_PRIVATE_KEY=0x...
